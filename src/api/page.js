//util
import {
  _,
  assign,
  eachKey,
  findValue,
  forEach,
  has,
  makeObjectFromKeyCollection,
  merge,

} from '../core/util'

//main
export default {
  find(params, cb, next){

  },
  one(params, cb, next){

  },
  save(postObj, cb, next){

  },
  kill(postId, cb, next){

  }
}
